<UserControl
    x:Class="PlaylistArchivizer.UI.WPF.Views.ErrorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PlaylistArchivizer.UI.WPF.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:ErrorViewModel}"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Content="Wystąpił błąd krytyczny"
            FontSize="40"
            FontWeight="Bold" />

        <Grid Grid.Row="1" Margin="10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                Margin="0,0,0,20"
                FontSize="30"
                Text="{Binding ExceptionTitle}"
                TextWrapping="Wrap" />

            <Expander Grid.Row="1">
                <Expander.Header>
                    <Label Content="Szczegóły" FontSize="20" />
                </Expander.Header>

                <Grid Margin="20,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Label
                        Content="{Binding ExceptionType}"
                        ContentStringFormat="{}{0}:"
                        FontSize="30" />

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <TextBlock
                            Margin="0,10,0,0"
                            FontSize="20"
                            Text="{Binding ExceptionDetails}"
                            TextWrapping="Wrap" />
                    </ScrollViewer>
                </Grid>
            </Expander>
        </Grid>

        <Button
            Grid.Row="2"
            Margin="20"
            Padding="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Command="{Binding CloseCommand}"
            Content="Kontynuuj"
            FontSize="20" />
    </Grid>
</UserControl>
